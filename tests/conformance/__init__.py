"""Conformance suite of the crypto research contract (C24.1 conformance_suite).

Runs against whatever `GarimpoInvestimentos` and `cripto-research` are installed, so the
same suite checks the checkout (CI) and the published wheel (cleanroom-final).
"""
