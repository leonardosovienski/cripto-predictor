.env
.cripto-root
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
dist/
build/
htmlcov/
data/
# Ancorado na raiz: um `output/` genérico tirava o pacote de código output/ da wheel (CR-F005).
/output/
cache/
logs/
*.db
*.db-wal
*.db-shm
*.jsonl
# Ledgers de evidência são versionados de propósito (execuções registradas, holdouts selados).
!docs/evidence/**/*.jsonl
!docs/research_ledger/*.jsonl

# Local research runtimes and regenerated outputs; maintained sources live in scripts/.
/work/
